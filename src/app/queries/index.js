import {
  GET_USERS, ADD_USER, EDIT_USER, DELETE_USER,
}  from './users/users.gql'

import {
  GET_POSTS, ADD_POST, 
}  from './posts/posts.gql'

import {
  GET_MAPS, 
}  from './maps/maps.gql'

export  {
  GET_USERS, ADD_USER, EDIT_USER, DELETE_USER,
  GET_POSTS, ADD_POST, 
  GET_MAPS,
}